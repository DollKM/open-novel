将文件写入本地文件系统。

用法：
- 如果提供的路径已存在文件，此工具将覆盖它。
- 如果是已有文件，你必须先使用 Read 工具读取文件内容。如果未先读取文件，此工具将失败。
- 始终优先编辑代码库中的现有文件。除非明确要求，绝不创建新文件。
- 绝不主动创建文档文件（*.md）或 README 文件。仅当用户明确要求时才创建文档文件。
- 仅在用户明确要求时使用 emoji。除非被要求，避免在文件中写入 emoji。
