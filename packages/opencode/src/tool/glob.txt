- 按名称模式匹配文件。优先使用 codegraph_files 浏览项目文件结构（基于 AST 索引，更快）；仅当需要按通配符匹配文件名时使用此工具。
- 适用于任何规模代码库的快速文件模式匹配工具
- 支持 glob 模式如 "**/*.js" 或 "src/**/*.ts"
- 返回匹配的文件路径
- 当需要按名称模式查找文件时使用此工具
- 当进行可能需要多轮 glob 和 grep 的开放式搜索时，请改用 Explore 代理
- 你可以在单次响应中调用多个工具。将潜在有用的多次搜索作为批量执行总是更好的做法。
